# 新手教程系统 v3.6 - 设计文档

## 更新日期
2024-12-19

## 功能概述

在玩家首次进入游戏时，显示一段简洁直观的玩法介绍，让新玩家能够快速理解游戏的核心机制和操作方式。

### 核心目标
1. **快速理解** - 3-5秒内理解核心玩法
2. **视觉直观** - 用图标和动画展示，而非大段文字
3. **可跳过** - 老玩家可以快速跳过
4. **不打断** - 不影响游戏流畅度

---

## 设计方案对比

### 方案A：静态图文教程 ⭐⭐⭐

#### 展示方式
```
┌─────────────────────────────────────┐
│        HOW TO PLAY                  │
│         游戏玩法                     │
├─────────────────────────────────────┤
│                                     │
│  🎮 WASD - 移动                     │
│  🛡️ 空格/鼠标 - 格挡                │
│  ⚔️ 格挡成功 → 自动反击             │
│  💀 被击中 → 游戏结束               │
│                                     │
│  目标：击杀尽可能多的敌人！          │
│                                     │
│  ┌──────────┐  ┌──────────┐        │
│  │  START   │  │  SKIP    │        │
│  └──────────┘  └──────────┘        │
└─────────────────────────────────────┘
```

**优点**:
- ✅ 实现简单
- ✅ 信息清晰
- ✅ 加载快速

**缺点**:
- ⚠️ 不够生动
- ⚠️ 缺乏互动感

---

### 方案B：分步动画教程 ⭐⭐⭐⭐⭐（推荐）

#### 展示方式
分3-4个步骤，每步配合动画演示：

**步骤1：移动**
```
┌─────────────────────────────────────┐
│        [1/4]                        │
│                                     │
│         🎮 移动                     │
│                                     │
│      [玩家图标动画]                 │
│         ↑                           │
│       ← ● →   WASD                  │
│         ↓                           │
│                                     │
│    使用 WASD 键移动角色              │
│                                     │
│         [下一步 →]                  │
└─────────────────────────────────────┘
```

**步骤2：格挡**
```
┌─────────────────────────────────────┐
│        [2/4]                        │
│                                     │
│         🛡️ 格挡                     │
│                                     │
│    [敌人] → [玩家+护盾]             │
│      ⚔️      🛡️                     │
│                                     │
│  按住 空格/鼠标 格挡敌人攻击         │
│                                     │
│  [← 上一步]  [下一步 →]             │
└─────────────────────────────────────┘
```

**步骤3：反击**
```
┌─────────────────────────────────────┐
│        [3/4]                        │
│                                     │
│         ⚔️ 反击                     │
│                                     │
│    [玩家] ⚡→ [敌人]                │
│      🛡️        💥                   │
│                                     │
│  格挡成功后自动瞬移反击敌人          │
│                                     │
│  [← 上一步]  [下一步 →]             │
└─────────────────────────────────────┘
```

**步骤4：目标**
```
┌─────────────────────────────────────┐
│        [4/4]                        │
│                                     │
│         🎯 目标                     │
│                                     │
│    击杀: 50  时间: 120s             │
│    连击: 15x                        │
│                                     │
│  击杀尽可能多的敌人                  │
│  存活尽可能长的时间                  │
│                                     │
│  [← 上一步]  [开始游戏 →]           │
└─────────────────────────────────────┘
```

**优点**:
- ✅ 生动直观
- ✅ 分步学习，不会信息过载
- ✅ 配合动画，更易理解
- ✅ 可以前后翻页

**缺点**:
- ⚠️ 实现稍复杂
- ⚠️ 需要设计动画

---

### 方案C：交互式教程 ⭐⭐⭐⭐

#### 展示方式
实际进入游戏场景，引导玩家完成操作：

```
第1步：移动教学
- 屏幕提示："按 WASD 移动"
- 玩家移动后进入下一步

第2步：格挡教学
- 生成1个敌人
- 提示："按空格格挡"
- 成功格挡后进入下一步

第3步：反击教学
- 提示："格挡成功会自动反击"
- 观看反击动画

第4步：完成
- 提示："教程完成，开始游戏！"
```

**优点**:
- ✅ 最直观
- ✅ 实际操作，印象深刻
- ✅ 互动性强

**缺点**:
- ⚠️ 实现复杂
- ⚠️ 耗时较长
- ⚠️ 可能打断游戏节奏

---

### 方案D：视频教程 ⭐⭐

#### 展示方式
播放一段10-15秒的游戏演示视频

**优点**:
- ✅ 最直观
- ✅ 展示实际游戏画面

**缺点**:
- ⚠️ 需要录制视频
- ⚠️ 文件体积大
- ⚠️ 不够灵活

---

## 最终方案：方案B（分步动画教程）✅

### 选择理由
1. ✅ **平衡性好** - 既直观又不复杂
2. ✅ **实现可行** - 使用 CSS 动画即可
3. ✅ **用户友好** - 分步学习，可前后翻页
4. ✅ **可跳过** - 不强制观看
5. ✅ **视觉统一** - 与游戏风格一致

### 用户确认配置
- ✅ **触发时机**: 仅首次访问
- ✅ **可跳过性**: 必须可跳过
- ✅ **自动播放**: 是（每步自动切换）
- ✅ **键盘快捷键**: 不需要
- ✅ **多语言**: 不需要
- ✅ **动画复杂度**: 中等

---

## 详细设计（方案B）

### 1. 触发时机

#### 首次进入（自动触发）
```javascript
// 检查是否首次进入
const hasSeenTutorial = localStorage.getItem('hasSeenTutorial');

if (!hasSeenTutorial) {
    showTutorial();
} else {
    showMainMenu();
}
```

#### 手动触发（可选）
```
主菜单添加 "?" 或 "教程" 按钮
点击后可重新查看教程
```

### 2. 教程步骤（共5步）

#### 步骤1：移动（5秒）
**标题**: 🎮 移动

**内容**:
- 图标：玩家角色（白色圆形）
- 动画：角色在四个方向移动（上下左右循环）
- 文字：使用 WASD 键移动角色

**动画效果**:
```css
@keyframes moveDemo {
    0%, 100% { transform: translate(0, 0); }
    20% { transform: translate(0, -40px); }  /* W - 上 */
    40% { transform: translate(-40px, 0); }  /* A - 左 */
    60% { transform: translate(0, 40px); }   /* S - 下 */
    80% { transform: translate(40px, 0); }   /* D - 右 */
}
```

**实现细节**:
- 玩家圆形持续在中心区域移动
- 显示半透明的 WASD 按键提示
- 移动轨迹留下淡淡的残影

#### 步骤2：格挡（5秒）
**标题**: 🛡️ 格挡

**内容**:
- 图标：远程敌人（红色三角形）+ 子弹 + 玩家（白色圆形+蓝色护盾）
- 动画：敌人发射子弹 → 子弹飞向玩家 → 玩家展开护盾 → 子弹被弹开
- 文字：按住 空格/鼠标 格挡敌人攻击

**动画效果**:
```css
@keyframes enemyShoot {
    0%, 100% { opacity: 1; }
    30% { transform: scale(1.2); } /* 发射动作 */
}

@keyframes bulletFly {
    0% { transform: translateX(-150px); opacity: 0; }
    20% { opacity: 1; }
    50% { transform: translateX(-20px); }
    60% { transform: translateX(-30px) scale(0.5); opacity: 0.5; } /* 被弹开 */
    100% { transform: translateX(-100px) translateY(50px); opacity: 0; }
}

@keyframes shieldAppear {
    0%, 40% { opacity: 0; transform: scale(0); }
    50%, 70% { opacity: 1; transform: scale(1); }
    100% { opacity: 0; transform: scale(0); }
}
```

**实现细节**:
- 远程敌人在左侧，玩家在右侧
- 敌人发射红色子弹
- 子弹飞向玩家时，玩家展开蓝色护盾
- 子弹被护盾弹开，向下飞出
- 护盾有脉冲光效

#### 步骤3：反击（5秒）
**标题**: ⚔️ 反击

**内容**:
- 图标：玩家瞬移到敌人位置 + 刀光效果
- 动画：格挡成功 → 玩家瞬移 → 刀光闪现 → 敌人爆炸消失
- 文字：格挡成功后自动瞬移反击敌人

**动画效果**:
```css
@keyframes counterTeleport {
    0%, 30% { transform: translateX(0); opacity: 1; }
    40% { opacity: 0; } /* 消失 */
    50% { transform: translateX(-150px); opacity: 0; }
    60% { opacity: 1; } /* 出现在敌人位置 */
    100% { transform: translateX(-150px); }
}

@keyframes slashEffect {
    0%, 50% { opacity: 0; transform: scale(0) rotate(0deg); }
    60% { opacity: 1; transform: scale(1.5) rotate(45deg); }
    70% { opacity: 0.5; }
    100% { opacity: 0; transform: scale(2) rotate(90deg); }
}

@keyframes enemyExplode {
    0%, 60% { opacity: 1; transform: scale(1); }
    70% { transform: scale(1.3); }
    80% { transform: scale(0.5); opacity: 0.5; }
    100% { transform: scale(0); opacity: 0; }
}
```

**实现细节**:
- 玩家从右侧瞬移到左侧敌人位置
- 瞬移过程有残影效果
- 到达后刀光闪现（白色/青色光效）
- 敌人爆炸成粒子消失
- 粒子向四周扩散

#### 步骤4：完美格挡（5秒）⭐ 新增
**标题**: ⚡ 完美格挡

**内容**:
- 图标：多个敌人 + 玩家连续反击
- 动画：敌人1攻击 → 完美格挡 → 反击敌人1 → 敌人2攻击 → 完美格挡 → 反击敌人2
- 文字：在敌人攻击瞬间格挡可触发多次反击

**动画效果**:
```css
@keyframes perfectParryFlash {
    0%, 100% { opacity: 0; }
    50% { opacity: 1; box-shadow: 0 0 30px #ffd700; }
}

@keyframes multiCounterChain {
    0% { transform: translate(0, 0); }
    20% { transform: translate(-100px, -50px); } /* 反击敌人1 */
    40% { transform: translate(0, 0); } /* 返回 */
    60% { transform: translate(-100px, 50px); } /* 反击敌人2 */
    80%, 100% { transform: translate(0, 0); } /* 返回 */
}

@keyframes goldFlash {
    0%, 100% { opacity: 0; }
    50% { opacity: 1; background: radial-gradient(circle, #ffd700, transparent); }
}
```

**实现细节**:
- 场景中有2-3个敌人
- 第一个敌人攻击时，玩家完美格挡（金色闪光）
- 玩家瞬移反击第一个敌人
- 第二个敌人立即攻击，玩家再次完美格挡
- 玩家连续瞬移反击第二个敌人
- 强调"攻击瞬间"的时机把握
- 金色光效区别于普通格挡的蓝色光效

#### 步骤5：目标（5秒）
**标题**: 🎯 目标

**内容**:
- 图标：击杀数、时间、连击数的UI展示
- 动画：数字从0递增，连击数闪烁
- 文字：击杀尽可能多的敌人，存活尽可能长的时间

**动画效果**:
```css
@keyframes statsCountUp {
    0% { opacity: 0; transform: scale(0.5); }
    50% { opacity: 1; transform: scale(1.2); }
    100% { transform: scale(1); }
}

@keyframes comboGlow {
    0%, 100% { text-shadow: 0 0 5px #fff; }
    50% { text-shadow: 0 0 20px #0ff, 0 0 30px #0ff; }
}
```

**实现细节**:
- 显示游戏UI的缩略版
- 击杀数从0跳到50
- 时间从0跳到120s
- 连击数从0跳到15x，带发光效果
- 强调"击杀数"和"存活时间"是核心目标

### 3. UI 设计

#### 整体布局
```
┌─────────────────────────────────────┐
│  [步骤指示器] 1 ● 2 ○ 3 ○ 4 ○ 5 ○  │  ← 顶部进度（5个点）
│                                     │
│         [标题图标 + 文字]           │  ← 大标题
│                                     │
│                                     │
│         [动画演示区域]              │  ← 中央动画
│                                     │
│                                     │
│         [说明文字]                  │  ← 底部说明
│                                     │
│  [← 上一步]  [跳过]  [下一步 →]    │  ← 底部按钮
│                                     │
│  自动播放中... 5秒后自动跳转        │  ← 自动播放提示
└─────────────────────────────────────┘
```

#### 样式设计
```css
/* 教程容器 */
#tutorialScreen {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background: rgba(0, 0, 0, 0.95);  /* 深色背景 */
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 2000;
}

/* 教程内容 */
.tutorial-content {
    background: rgba(245, 245, 245, 0.98);
    padding: 60px;
    border: 2px solid #000;
    border-radius: 12px;
    width: 600px;
    text-align: center;
}

/* 步骤指示器 */
.tutorial-steps {
    display: flex;
    justify-content: center;
    gap: 15px;
    margin-bottom: 30px;
}

.step-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: #CCC;
    transition: all 0.3s;
}

.step-dot.active {
    background: #000;
    transform: scale(1.5);
}

/* 标题 */
.tutorial-title {
    font-size: 48px;
    margin-bottom: 40px;
    color: #000;
}

/* 动画区域 */
.tutorial-animation {
    height: 200px;
    margin: 40px 0;
    position: relative;
}

/* 说明文字 */
.tutorial-description {
    font-size: 20px;
    color: #666;
    margin-bottom: 40px;
    line-height: 1.6;
}

/* 按钮组 */
.tutorial-buttons {
    display: flex;
    justify-content: space-between;
    gap: 15px;
}

.tutorial-button {
    padding: 12px 30px;
    font-size: 18px;
    border: 2px solid #000;
    background: transparent;
    color: #000;
    border-radius: 25px;
    cursor: pointer;
    transition: all 0.3s;
}

.tutorial-button:hover {
    background: #000;
    color: #F5F5F5;
}

.tutorial-button.primary {
    background: #000;
    color: #F5F5F5;
}

.tutorial-button.primary:hover {
    background: #333;
}
```

### 4. 动画实现

#### 移动动画
```html
<div class="tutorial-animation">
    <div class="demo-player" id="movePlayer">
        <div class="player-circle"></div>
        <div class="wasd-keys">
            <div class="key">W</div>
            <div class="key">A</div>
            <div class="key">S</div>
            <div class="key">D</div>
        </div>
    </div>
</div>
```

```css
.demo-player {
    animation: moveDemo 4s ease-in-out infinite;
}

@keyframes moveDemo {
    0%, 100% { transform: translate(0, 0); }
    25% { transform: translate(0, -40px); }
    50% { transform: translate(-40px, 0); }
    75% { transform: translate(0, 40px); }
}
```

#### 格挡动画
```html
<div class="tutorial-animation">
    <div class="demo-enemy"></div>
    <div class="demo-player-block">
        <div class="player-circle"></div>
        <div class="shield"></div>
    </div>
</div>
```

```css
.demo-enemy {
    animation: enemyApproach 3s ease-in-out infinite;
}

.shield {
    animation: shieldAppear 3s ease-in-out infinite;
}

@keyframes enemyApproach {
    0%, 100% { transform: translateX(-100px); opacity: 1; }
    50% { transform: translateX(-20px); }
    60% { transform: translateX(-30px); opacity: 0.5; }
}

@keyframes shieldAppear {
    0%, 40% { opacity: 0; transform: scale(0); }
    50%, 70% { opacity: 1; transform: scale(1); }
    100% { opacity: 0; }
}
```

#### 反击动画
```html
<div class="tutorial-animation">
    <div class="demo-player-counter"></div>
    <div class="demo-enemy-target"></div>
    <div class="slash-effect"></div>
</div>
```

```css
.demo-player-counter {
    animation: counterAttack 3s ease-in-out infinite;
}

.slash-effect {
    animation: slashAppear 3s ease-in-out infinite;
}

@keyframes counterAttack {
    0%, 100% { transform: translateX(0); }
    50%, 80% { transform: translateX(100px); }
}

@keyframes slashAppear {
    0%, 40% { opacity: 0; }
    50% { opacity: 1; }
    60%, 100% { opacity: 0; }
}
```

### 5. 交互逻辑

#### JavaScript 实现
```javascript
class TutorialManager {
    constructor() {
        this.currentStep = 0;
        this.totalSteps = 5; // 更新为5步
        this.autoPlayTimer = null;
        this.autoPlayDuration = 5000; // 每步5秒
        this.steps = [
            {
                title: '🎮 移动',
                description: '使用 WASD 键移动角色',
                animation: 'move'
            },
            {
                title: '🛡️ 格挡',
                description: '按住 空格/鼠标 格挡敌人攻击',
                animation: 'block'
            },
            {
                title: '⚔️ 反击',
                description: '格挡成功后自动瞬移反击敌人',
                animation: 'counter'
            },
            {
                title: '⚡ 完美格挡',
                description: '在敌人攻击瞬间格挡可触发多次反击',
                animation: 'perfectParry'
            },
            {
                title: '🎯 目标',
                description: '击杀尽可能多的敌人\n存活尽可能长的时间',
                animation: 'goal'
            }
        ];
    }
    
    show() {
        document.getElementById('tutorialScreen').style.display = 'flex';
        this.showStep(0);
    }
    
    showStep(step) {
        // 清除之前的自动播放计时器
        if (this.autoPlayTimer) {
            clearTimeout(this.autoPlayTimer);
        }
        
        this.currentStep = step;
        const stepData = this.steps[step];
        
        // 更新标题
        document.getElementById('tutorialTitle').textContent = stepData.title;
        
        // 更新说明
        document.getElementById('tutorialDescription').textContent = stepData.description;
        
        // 更新进度指示器
        this.updateStepIndicator();
        
        // 显示对应动画
        this.showAnimation(stepData.animation);
        
        // 更新按钮状态
        this.updateButtons();
        
        // 启动自动播放
        this.startAutoPlay();
    }
    
    startAutoPlay() {
        // 设置自动播放计时器
        this.autoPlayTimer = setTimeout(() => {
            this.nextStep();
        }, this.autoPlayDuration);
    }
    
    stopAutoPlay() {
        if (this.autoPlayTimer) {
            clearTimeout(this.autoPlayTimer);
            this.autoPlayTimer = null;
        }
    }
    
    nextStep() {
        this.stopAutoPlay();
        
        if (this.currentStep < this.totalSteps - 1) {
            this.showStep(this.currentStep + 1);
        } else {
            this.complete();
        }
    }
    
    prevStep() {
        if (this.currentStep > 0) {
            this.showStep(this.currentStep - 1);
        }
    }
    
    skip() {
        this.stopAutoPlay();
        this.complete();
    }
    
    complete() {
        this.stopAutoPlay();
        
        // 标记已看过教程
        localStorage.setItem('hasSeenTutorial', 'true');
        
        // 隐藏教程
        document.getElementById('tutorialScreen').style.display = 'none';
        
        // 显示主菜单
        document.getElementById('startScreen').style.display = 'block';
    }
    
    updateStepIndicator() {
        const dots = document.querySelectorAll('.step-dot');
        dots.forEach((dot, index) => {
            if (index === this.currentStep) {
                dot.classList.add('active');
            } else {
                dot.classList.remove('active');
            }
        });
    }
    
    updateButtons() {
        const prevBtn = document.getElementById('tutorialPrevBtn');
        const nextBtn = document.getElementById('tutorialNextBtn');
        
        // 第一步隐藏"上一步"
        prevBtn.style.visibility = this.currentStep === 0 ? 'hidden' : 'visible';
        
        // 最后一步改为"开始游戏"
        nextBtn.textContent = this.currentStep === this.totalSteps - 1 ? '开始游戏' : '下一步';
    }
    
    showAnimation(type) {
        // 隐藏所有动画
        document.querySelectorAll('.tutorial-animation > div').forEach(el => {
            el.style.display = 'none';
        });
        
        // 显示对应动画
        document.getElementById(`animation-${type}`).style.display = 'block';
    }
}

// 创建全局实例
const tutorial = new TutorialManager();
```

### 6. 配置选项

#### config.json 新增
```json
"tutorial": {
  "enabled": true,
  "autoShow": true,
  "autoPlay": true,
  "autoPlayDuration": 5000,
  "canSkip": true,
  "showOnFirstVisit": true,
  "keyboardShortcuts": false,
  "steps": [
    {
      "id": "move",
      "title": "🎮 移动",
      "description": "使用 WASD 键移动角色",
      "duration": 5000
    },
    {
      "id": "block",
      "title": "🛡️ 格挡",
      "description": "按住 空格/鼠标 格挡敌人攻击",
      "duration": 5000
    },
    {
      "id": "counter",
      "title": "⚔️ 反击",
      "description": "格挡成功后自动瞬移反击敌人",
      "duration": 5000
    },
    {
      "id": "perfectParry",
      "title": "⚡ 完美格挡",
      "description": "在敌人攻击瞬间格挡可触发多次反击",
      "duration": 5000
    },
    {
      "id": "goal",
      "title": "🎯 目标",
      "description": "击杀尽可能多的敌人\n存活尽可能长的时间",
      "duration": 5000
    }
  ]
}
```

### 7. 主菜单集成

#### 添加教程按钮
```html
<div id="startScreen">
    <h1>BLADE ECHO</h1>
    <div class="subtitle">剑刃回响</div>
    
    <button onclick="showDifficultySelect()">START</button>
    <button onclick="showLeaderboard()">RANKS</button>
    <button onclick="tutorial.show()">? 教程</button>  ← 新增
    
    <div class="hint">WASD 移动 · 空格/鼠标 格挡</div>
</div>
```

### 8. 实现步骤

#### 第一阶段：HTML 结构（30分钟）
1. [ ] 创建教程界面 HTML
2. [ ] 添加步骤指示器（5个点）
3. [ ] 添加5个动画容器
4. [ ] 添加按钮组和自动播放提示

#### 第二阶段：CSS 样式（1小时）
1. [ ] 教程容器样式
2. [ ] 步骤指示器样式
3. [ ] 动画区域样式
4. [ ] 按钮样式
5. [ ] 自动播放进度条样式

#### 第三阶段：动画实现（2小时）⭐ 重点
1. [ ] 步骤1：移动动画（WASD循环移动）
2. [ ] 步骤2：格挡动画（敌人发射子弹+护盾弹开）
3. [ ] 步骤3：反击动画（瞬移+刀光+爆炸）
4. [ ] 步骤4：完美格挡动画（多次反击链+金色光效）
5. [ ] 步骤5：目标动画（数字递增+发光）

#### 第四阶段：JavaScript 逻辑（1小时）
1. [ ] TutorialManager 类（包含自动播放）
2. [ ] 步骤切换逻辑
3. [ ] 自动播放计时器
4. [ ] 首次访问检测
5. [ ] 完成和跳过逻辑

#### 第五阶段：集成测试（30分钟）
1. [ ] 首次访问自动显示测试
2. [ ] 自动播放测试（5秒自动切换）
3. [ ] 手动切换测试（上一步/下一步）
4. [ ] 跳过功能测试
5. [ ] 动画流畅度测试
6. [ ] 重新查看测试（主菜单按钮）

**总计：约 5 小时**

---

## 核心功能实现

### 1. 自动播放 ✅ 必须实现
```javascript
// 自动切换到下一步
startAutoPlay() {
    this.autoPlayTimer = setTimeout(() => {
        this.nextStep();
    }, this.autoPlayDuration); // 5000ms
}

// 停止自动播放（用户手动操作时）
stopAutoPlay() {
    if (this.autoPlayTimer) {
        clearTimeout(this.autoPlayTimer);
        this.autoPlayTimer = null;
    }
}
```

### 2. 跳过功能 ✅ 必须实现
```javascript
skip() {
    this.stopAutoPlay();
    this.complete();
}
```

### 3. 首次访问检测 ✅ 必须实现
```javascript
// 检查是否首次访问
const hasSeenTutorial = localStorage.getItem('hasSeenTutorial');

if (!hasSeenTutorial) {
    tutorial.show(); // 自动显示教程
} else {
    showMainMenu(); // 直接显示主菜单
}
```

## 可选功能（暂不实现）

### 1. 键盘快捷键 ❌ 不需要
用户确认不需要键盘快捷键

### 2. 多语言支持 ❌ 不需要
用户确认不需要多语言

### 3. 进度保存 ❌ 暂不需要
教程较短（5步），无需保存进度

---

## 用户体验考虑

### 优点
- ✅ 新玩家快速上手
- ✅ 降低学习曲线
- ✅ 提升留存率
- ✅ 减少困惑和挫败感

### 注意事项
- ⚠️ 不要太长（总时长 < 30秒）
- ⚠️ 必须可跳过
- ⚠️ 老玩家不要重复看到
- ⚠️ 动画要流畅

---

## 测试清单

### 功能测试
- [ ] 首次访问自动显示
- [ ] 步骤切换正常
- [ ] 动画播放正常
- [ ] 跳过功能正常
- [ ] 完成后不再显示

### 视觉测试
- [ ] 动画流畅
- [ ] 文字清晰
- [ ] 布局美观
- [ ] 响应式正常

### 交互测试
- [ ] 按钮点击正常
- [ ] 键盘快捷键正常
- [ ] 进度指示器正常

---

## 设计确认清单 ✅

### 已确认内容
1. ✅ **方案选择**：方案B（分步动画教程）
2. ✅ **步骤数量**：5步（移动 → 格挡 → 反击 → 完美格挡 → 目标）
3. ✅ **触发时机**：仅首次访问
4. ✅ **可跳过性**：必须可跳过
5. ✅ **自动播放**：是（每步5秒自动切换）
6. ✅ **键盘快捷键**：不需要
7. ✅ **多语言**：不需要
8. ✅ **动画复杂度**：中等

### 步骤内容确认
- ✅ **步骤1**：移动（WASD循环移动）
- ✅ **步骤2**：格挡（远程敌人发射子弹 + 玩家护盾弹开）
- ✅ **步骤3**：反击（瞬移 + 刀光 + 敌人爆炸）
- ✅ **步骤4**：完美格挡（多次反击链 + 金色光效）⭐ 新增
- ✅ **步骤5**：目标（击杀数和时间展示）

### 技术实现要点
1. **自动播放系统**：每步5秒后自动切换到下一步
2. **手动控制**：用户可随时点击"上一步"/"下一步"/"跳过"
3. **首次检测**：使用 localStorage 记录是否已看过
4. **动画质量**：中等复杂度，流畅且易懂
5. **视觉风格**：与游戏主体风格一致（黑白灰 + 彩色点缀）

---

## 下一步行动

**文档版本**: v3.6  
**最后更新**: 2024-12-19  
**状态**: 设计完成，等待用户最终确认 ⏳

### 请确认：
1. 步骤2的动画描述是否符合预期？（远程敌人发射子弹 + 护盾弹开）
2. 步骤4的完美格挡动画是否清晰？（多次反击链 + 金色光效）
3. 5步的顺序和内容是否完整？

**确认无误后，我将立即开始实现代码！** 🚀
