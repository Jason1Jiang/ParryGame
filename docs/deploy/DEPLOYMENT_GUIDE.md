# Netlify 部署指南

## 部署方式

你有3种方式部署到 Netlify：

---

## 方式1：通过 Git（推荐）⭐

### 步骤

#### 1. 准备 Git 仓库

如果还没有 Git 仓库：

```bash
# 初始化 Git
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"
```

#### 2. 推送到 GitHub/GitLab

**GitHub**：
```bash
# 创建 GitHub 仓库后
git remote add origin https://github.com/你的用户名/ParryGame.git
git branch -M main
git push -u origin main
```

**GitLab**：
```bash
git remote add origin https://gitlab.com/你的用户名/ParryGame.git
git branch -M main
git push -u origin main
```

#### 3. 连接 Netlify

1. 访问 https://app.netlify.com
2. 点击 "Add new site" → "Import an existing project"
3. 选择 GitHub/GitLab
4. 授权并选择你的仓库
5. 配置构建设置：
   - **Build command**: 留空
   - **Publish directory**: `.`（点号，表示根目录）
6. 点击 "Deploy site"

#### 4. 完成！

- Netlify 会自动部署
- 每次推送代码都会自动重新部署
- 获得一个 `.netlify.app` 域名

---

## 方式2：拖拽部署（最简单）🚀

### 步骤

#### 1. 准备文件

确保项目文件夹包含：
```
ParryGame/
├── index.html
├── game.js
├── config.json
├── assets/
│   └── music/
│       └── bgm_battle.mp3
└── netlify.toml
```

#### 2. 部署

1. 访问 https://app.netlify.com/drop
2. 直接拖拽整个 `ParryGame` 文件夹到页面
3. 等待上传完成
4. 完成！获得一个 `.netlify.app` 域名

**优点**：
- ✅ 最简单
- ✅ 无需 Git
- ✅ 立即部署

**缺点**：
- ⚠️ 每次更新需要重新拖拽
- ⚠️ 无法自动部署

---

## 方式3：Netlify CLI（命令行）💻

### 步骤

#### 1. 安装 Netlify CLI

```bash
npm install -g netlify-cli
```

#### 2. 登录

```bash
netlify login
```

#### 3. 初始化

```bash
# 在项目目录中
netlify init
```

#### 4. 部署

```bash
# 部署到生产环境
netlify deploy --prod
```

**优点**：
- ✅ 命令行控制
- ✅ 可以预览部署
- ✅ 适合开发者

---

## 音乐文件处理

### 选项1：包含在仓库中

**优点**：
- ✅ 部署后直接可用
- ✅ 无需额外配置

**缺点**：
- ⚠️ 增加仓库大小
- ⚠️ 如果音乐文件很大，可能影响克隆速度

**操作**：
```bash
# 确保音乐文件在 assets/music/ 中
git add assets/music/bgm_battle.mp3
git commit -m "Add battle music"
git push
```

---

### 选项2：使用 Git LFS（大文件）

如果音乐文件很大（>5MB），建议使用 Git LFS：

```bash
# 安装 Git LFS
git lfs install

# 追踪 MP3 文件
git lfs track "*.mp3"

# 添加 .gitattributes
git add .gitattributes

# 添加音乐文件
git add assets/music/bgm_battle.mp3
git commit -m "Add battle music with LFS"
git push
```

---

### 选项3：外部托管（CDN）

将音乐文件托管在其他地方：

**免费 CDN 选项**：
- Cloudinary
- Imgur（音频）
- Google Drive（公开链接）

**修改 config.json**：
```json
"music": {
  "files": {
    "battle": "https://你的CDN地址/bgm_battle.mp3"
  }
}
```

---

## 部署后配置

### 1. 自定义域名（可选）

在 Netlify 控制台：
1. 进入 "Domain settings"
2. 点击 "Add custom domain"
3. 输入你的域名
4. 按照提示配置 DNS

---

### 2. HTTPS

Netlify 自动提供免费 HTTPS：
- ✅ 自动配置
- ✅ Let's Encrypt 证书
- ✅ 自动续期

---

### 3. 环境变量（如果需要）

在 Netlify 控制台：
1. 进入 "Site settings" → "Environment variables"
2. 添加变量
3. 重新部署

---

## 优化建议

### 1. 压缩音乐文件

使用工具压缩 MP3：
- **Audacity**（免费）
- **在线工具**：https://www.mp3smaller.com

**推荐设置**：
- 比特率：128 kbps
- 采样率：44.1 kHz
- 目标大小：< 3 MB

---

### 2. 启用缓存

`netlify.toml` 已配置缓存：
- HTML/JSON：1小时
- JS/CSS/音频：1年（immutable）

---

### 3. 性能优化

**已包含在 netlify.toml**：
- ✅ 缓存控制
- ✅ 安全头部
- ✅ 内容类型设置

---

## 测试部署

### 本地测试

使用 Netlify CLI 本地测试：

```bash
# 安装 CLI
npm install -g netlify-cli

# 本地运行
netlify dev
```

---

### 部署预览

```bash
# 部署到预览环境
netlify deploy

# 查看预览 URL
# 确认无误后部署到生产
netlify deploy --prod
```

---

## 常见问题

### Q: 音乐文件不播放？

**检查**：
1. 文件路径是否正确
2. 文件是否上传
3. 浏览器控制台是否有错误
4. 检查 CORS 设置

**解决**：
```toml
# 在 netlify.toml 中添加
[[headers]]
  for = "/*.mp3"
  [headers.values]
    Access-Control-Allow-Origin = "*"
```

---

### Q: 部署后样式错误？

**检查**：
1. 文件路径是否使用相对路径
2. 是否有大小写问题（Linux 区分大小写）

---

### Q: 如何回滚到之前的版本？

在 Netlify 控制台：
1. 进入 "Deploys"
2. 找到之前的部署
3. 点击 "Publish deploy"

---

### Q: 如何查看部署日志？

在 Netlify 控制台：
1. 进入 "Deploys"
2. 点击具体的部署
3. 查看 "Deploy log"

---

## 推荐工作流

### 开发 → 部署流程

```bash
# 1. 本地开发
# 修改代码...

# 2. 测试
# 打开 index.html 测试

# 3. 提交到 Git
git add .
git commit -m "描述你的修改"
git push

# 4. 自动部署
# Netlify 自动检测并部署

# 5. 验证
# 访问你的 .netlify.app 域名测试
```

---

## 部署清单

### 部署前检查

- [ ] 音乐文件已添加（`assets/music/bgm_battle.mp3`）
- [ ] `config.json` 配置正确
- [ ] 本地测试通过
- [ ] 所有文件已提交到 Git（如果使用 Git）
- [ ] `.gitignore` 配置正确
- [ ] `netlify.toml` 已创建

### 部署后检查

- [ ] 网站可以访问
- [ ] 游戏可以正常运行
- [ ] 音乐可以播放
- [ ] 所有功能正常
- [ ] 在不同浏览器测试
- [ ] 在移动端测试（可选）

---

## 成本

### Netlify 免费套餐

- ✅ 100 GB 带宽/月
- ✅ 300 分钟构建时间/月
- ✅ 无限网站
- ✅ HTTPS
- ✅ 自动部署

**对于你的游戏项目完全够用！**

---

## 示例部署命令

### 完整流程（Git 方式）

```bash
# 1. 初始化 Git（如果还没有）
git init

# 2. 添加所有文件
git add .

# 3. 提交
git commit -m "Ready for deployment"

# 4. 添加远程仓库（GitHub）
git remote add origin https://github.com/你的用户名/ParryGame.git

# 5. 推送
git branch -M main
git push -u origin main

# 6. 在 Netlify 网站上连接仓库
# 访问 https://app.netlify.com
# 点击 "Add new site" → "Import an existing project"
# 选择你的仓库

# 7. 完成！
```

---

## 获取帮助

### Netlify 文档
- https://docs.netlify.com

### Netlify 社区
- https://answers.netlify.com

### 常见问题
- https://docs.netlify.com/configure-builds/common-configurations/

---

## 下一步

1. **选择部署方式**（推荐方式1或方式2）
2. **准备音乐文件**（下载 bgm_battle.mp3）
3. **执行部署**
4. **测试网站**
5. **分享链接**！🎉

---

**祝部署顺利！** 🚀

如有问题，随时询问！
